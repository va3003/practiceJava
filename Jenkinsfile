pipeline {
    stages {
        agent any 
        stage('build') {
            steps {
                echo 'hello firstpipeline start'
                sh 'mvn --version'
                echo 'hello firstpipeline end'
            }
        }
    }
}
